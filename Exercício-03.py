# Declaração de variáveis 
altura: float = 0.0
base: float = 0.0

# Início 

altura = float(input('Qual é a altura do triângulo? '))
base = float(input('Qual é a base do triângulo? '))
print('Assim sendo, sua área é de', altura * base / 2, 'unidades quadradas.')

# Fim