# Declaração de variáveis 
a: float = 0.0
b: float = 0.0
diferença: float = 0.0


# Início 

a = float(input('Digite um valor real para a: '))
b = float(input('Digite um valor real para b: '))

diferença = a - b

print('A diferença entre estes dois valores reais é', diferença)

# Fim